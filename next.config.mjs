/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            hostname: "tame-cow-232.convex.cloud",
          },
        ],
      },
};

export default nextConfig;
  